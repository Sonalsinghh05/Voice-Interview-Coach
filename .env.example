# Copy this file to .env and fill in your values

# Required if FEEDBACK_MODE=cloud (default)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# "cloud" uses the Claude API, "local" uses Ollama running on your machine
FEEDBACK_MODE=cloud

# Only used when FEEDBACK_MODE=local
OLLAMA_MODEL=llama3.1
